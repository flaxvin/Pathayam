{
  "name": "pathayam",
  "version": "0.1.0",
  "private": true,
  "description": "Pathayam \u2014 strict envelope budgeting for one Indian household. Server-only, self-hosted.",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test --experimental-strip-types 'src/**/*.test.ts'",
    "dev": "node --experimental-strip-types --watch src/main.ts",
    "start": "node dist/main.js",
    "seed": "node --experimental-strip-types src/seed.ts",
    "demo": "node --experimental-strip-types src/demo.ts",
    "shots": "node tools/shoot.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
